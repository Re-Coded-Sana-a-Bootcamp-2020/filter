// You have an array with negative and positive number, filter them and push postive number to the positives array.

const numbers = [-1, -4, 3, 8];

let positives = [];

// A smaple solution might be like this.
// numbers.forEach((n) => {
//   if (n > 0) {
//     positives.push(n);
//   }
// });


// Creat the above result using .filter method
